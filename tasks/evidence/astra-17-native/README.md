# Native Step16+17 confirmation

Original full-text results, source archives and binaries:
/private/tmp/ze-astra17-native-mmcjqffv

24 processes, all648 queries per process, all exits0. Compact `.json.gz` files
retain every timing and hit identity/component-score bit, replacing text with
SHA256 and repeated hit epochs with the proven-equal top-level epoch. Full raw
file SHA256 receipts and exact full-payload comparison remain recorded. Tokens
are equality-checked across all24 files and represented by their verified digest.
The original model/index files and result payloads are not overwritten.
