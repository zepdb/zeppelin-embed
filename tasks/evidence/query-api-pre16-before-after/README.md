This is the compact archive of the completed matched three-API experiment.
Original source archives, binaries, stores, models and full per-query outputs:
`/private/tmp/ze-query-pre16-ielk83g_`.

`source.json` records each Git revision and adapter/harness hashes. Recreate
source using those immutable revisions, apply the corresponding lossless gzip
adapter patch, and install `harness/` as `tools/matched-api/` in each archive.
The original root retains the complete expanded sources and Git tar archives.
The three manifests, lockfiles and timing source were byte-identical; one copy
is retained here. Build/run/analyze scripts are preserved as executed, with
original paths. A rerun needs fresh roots and output paths; never overwrite
completed raw artifacts. Full text/score/token results stay in the original
root. Resource logs and source patches are lossless gzip archives.

The parent independently checked all 27 success receipts and recomputed all
nine median-process p50/p95 cells from the 17,496 full-query samples. No new
application change or model/retrieval default is part of this evidence archive.
